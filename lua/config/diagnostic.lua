vim.diagnostic.config({
  -- virtual_text = false,
  -- -- virtual_text = {
  -- --   prefix = '', -- Custom character for virtual text
  -- --   source = "always", -- Show source only if there are multiple sources
  -- -- },
  -- -- virtual_lines = true,
  -- float = {
  --   source = "always",
  -- },
  -- signs = true,
  underline = false,
  -- update_in_insert = true, -- Disable diagnostics while typing (improves performance)
})
